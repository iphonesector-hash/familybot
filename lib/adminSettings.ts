import { createClient } from "@supabase/supabase-js";

export type AdminGroupSettings={anti_flood:boolean;anti_link:boolean;flood_limit:number;flood_window_seconds:number;flood_mute_minutes:number;warn_limit:number;welcome_enabled:boolean};
const defaults:AdminGroupSettings={anti_flood:true,anti_link:false,flood_limit:5,flood_window_seconds:5,flood_mute_minutes:10,warn_limit:3,welcome_enabled:true};
function db(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error("Family Core database is not configured");return createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}})}
export async function readAdminSettings(familyId:string){const {data,error}=await db().from("group_settings").select("anti_flood,anti_link,flood_limit,flood_window_seconds,flood_mute_minutes,warn_limit,welcome_enabled").eq("family_id",familyId).maybeSingle();if(error)throw error;return {...defaults,...(data??{})}}
export async function writeAdminSettings(familyId:string,input:Partial<AdminGroupSettings>){const clean:AdminGroupSettings={anti_flood:Boolean(input.anti_flood),anti_link:Boolean(input.anti_link),welcome_enabled:Boolean(input.welcome_enabled),flood_limit:Math.max(3,Math.min(20,Number(input.flood_limit)||5)),flood_window_seconds:Math.max(2,Math.min(30,Number(input.flood_window_seconds)||5)),flood_mute_minutes:Math.max(1,Math.min(10080,Number(input.flood_mute_minutes)||10)),warn_limit:Math.max(1,Math.min(10,Number(input.warn_limit)||3))};const {data,error}=await db().from("group_settings").upsert({family_id:familyId,...clean},{onConflict:"family_id"}).select("anti_flood,anti_link,flood_limit,flood_window_seconds,flood_mute_minutes,warn_limit,welcome_enabled").single();if(error)throw error;return data as AdminGroupSettings}
