"use client";
import {usePathname} from "next/navigation";import {Icon,type IconName} from "./ui";
const items:[string,string,IconName,(path:string)=>boolean][]=[["/","خانه","home",p=>p==="/"],["/section/sagool","سگول","spark",p=>p.startsWith("/section/sagool")||p.startsWith("/section/house")||p.startsWith("/section/store")],["/section/games","بازی‌ها","games",p=>p.startsWith("/section/games")||p.startsWith("/section/multiplayer")||p.startsWith("/section/mafia")||p.startsWith("/section/game-guide")],["/ai","سکتور AI","ai",p=>p.startsWith("/ai")],["/section/leaderboard","پروفایل","profile",p=>p.startsWith("/section/leaderboard")||p.startsWith("/section/family")]];
export default function BottomNav(){const pathname=usePathname()||"/";return <nav className="bottomNav" aria-label="ناوبری اصلی">{items.map(([href,label,icon,match])=>{const active=match(pathname);return <a href={href} key={href} className={active?"active":""} aria-current={active?"page":undefined}><Icon name={icon}/><span>{label}</span></a>})}</nav>}
