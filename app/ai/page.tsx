"use client";
import {FormEvent,useEffect,useRef,useState} from "react";import {Icon,Mascot,type MascotMood} from "../ui";
type Msg={role:"user"|"assistant";content:string};const INTRO="درود بر شما من هوش مصنوعی سکتور هستم چطور میتونم کمکتون کنم؟";const quick=[["calendar","ثبت برنامه","دورهمی جمعه ساعت ۲۰ ثبت کن"],["birthday","تولدهای نزدیک","تولدهای نزدیک رو بهم بگو"],["poll","نظرسنجی","نظرسنجی شام کجا باشه گزینه‌ها: خونه، رستوران بساز"],["spark","جستجوی اینترنت","در اینترنت آخرین خبر مهم فناوری امروز رو جستجو کن"]] as const;
export default function AiPage(){const[msgs,setMsgs]=useState<Msg[]>([{role:"assistant",content:INTRO}]),[input,setInput]=useState(""),[busy,setBusy]=useState(false),[listening,setListening]=useState(false),[speaking,setSpeaking]=useState(false),[toast,setToast]=useState(""),[ready,setReady]=useState(false);const recorder=useRef<MediaRecorder|null>(null),chunks=useRef<Blob[]>([]),audio=useRef<HTMLAudioElement|null>(null);function token(){return sessionStorage.getItem("familybot.session")||""}function notify(t:string){setToast(t);setTimeout(()=>setToast(""),2300)}async function speak(text:string){const s=token();if(!s)return;audio.current?.pause();try{const r=await fetch("/api/voice/tts",{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${s}`},body:JSON.stringify({text})});if(!r.ok)throw 0;const u=URL.createObjectURL(await r.blob()),a=new Audio(u);audio.current=a;a.onplay=()=>setSpeaking(true);a.onended=()=>{setSpeaking(false);URL.revokeObjectURL(u)};await a.play()}catch{if("speechSynthesis" in window){const u=new SpeechSynthesisUtterance(text);u.lang="fa-IR";u.onstart=()=>setSpeaking(true);u.onend=()=>setSpeaking(false);window.speechSynthesis.cancel();window.speechSynthesis.speak(u)}}}async function send(e?:FormEvent,valueOverride?:string){e?.preventDefault();const value=(valueOverride??input).trim(),s=token();if(!value||busy)return;if(!s)return notify("Mini App را از داخل بله باز کن");setMsgs(v=>[...v,{role:"user",content:value}]);setInput("");setBusy(true);try{const r=await fetch("/api/ai/chat",{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${s}`},body:JSON.stringify({message:value,history:msgs.slice(-10)})}),x=await r.json();if(!r.ok)throw 0;const reply=String(x.reply||"پاسخی دریافت نشد");setMsgs(v=>[...v,{role:"assistant",content:reply}]);void speak(reply)}catch{notify("ارتباط با سکتور AI برقرار نشد")}finally{setBusy(false)}}async function transcribe(blob:Blob){const s=token();if(!s)return;setBusy(true);try{const f=new FormData();f.set("audio",new File([blob],"voice.webm",{type:blob.type||"audio/webm"}));const r=await fetch("/api/voice/stt",{method:"POST",headers:{authorization:`Bearer ${s}`},body:f}),x=await r.json();if(!r.ok||!x.text)throw 0;setBusy(false);await send(undefined,String(x.text))}catch{setBusy(false);notify("صدات واضح دریافت نشد")}}async function toggle(){if(listening){recorder.current?.stop();setListening(false);return}if(!navigator.mediaDevices?.getUserMedia||typeof MediaRecorder==="undefined")return notify("ضبط صدا روی این نسخه آماده نیست");try{const stream=await navigator.mediaDevices.getUserMedia({audio:true}),r=new MediaRecorder(stream);chunks.current=[];r.ondataavailable=e=>{if(e.data.size)chunks.current.push(e.data)};r.onstop=()=>{stream.getTracks().forEach(t=>t.stop());void transcribe(new Blob(chunks.current,{type:r.mimeType||"audio/webm"}))};recorder.current=r;r.start();setListening(true)}catch{notify("اجازه میکروفون صادر نشد")}}useEffect(()=>setReady(Boolean(token())),[]);const mood:MascotMood=listening?"listening":busy?"thinking":speaking?"speaking":"idle";return <main className="appShell aiScreen"><div className="ambient ambientA"/><div className="starField"/><div className={`motionToast${toast?" show":""}`}>{toast}</div><header className="aiTop"><a className="roundButton" href="/">←</a><div className="aiTitle"><h1>سکتور AI</h1><p>حافظه · اینترنت · صدای فارسی</p></div><span className="profileAvatar"><Icon name="ai"/></span></header>{!ready&&<div className="adminNotice">🔐 سکتور AI را از Mini App داخل بله باز کن.</div>}<section className={`aiStage premiumPanel${listening?" isListening":""}`}><Mascot mood={mood}/><h2>{INTRO}</h2><p>چت می‌کنم، اطلاعات روز را جستجو می‌کنم، حافظه گفتگو دارم و فرمان‌های Family Core را اجرا می‌کنم.</p><div className={`voiceWave ${listening?"active":busy?"thinking":speaking?"speaking":""}`}><i/><i/><i/><i/><i/><i/></div><button className={`voiceCircle${listening?" listening":""}`} onClick={()=>void toggle()} disabled={!ready||busy}><Icon name="ai" size={28}/></button></section><section className="chatList">{msgs.slice(-12).map((m,i)=><div className={`chatBubble ${m.role}`} key={i}><span>{m.content}</span>{m.role==="assistant"&&<button onClick={()=>void speak(m.content)}>◖))</button>}</div>)}</section><div className="quickTitle"><h3>فرمان‌های سریع</h3></div><div className="quickGrid">{quick.map(([icon,label,prompt])=><button className="quickCard" key={label} disabled={busy||!ready} onClick={()=>void send(undefined,prompt)}><span><Icon name={icon}/></span><b>{label}</b></button>)}</div><form className="composer" onSubmit={e=>void send(e)}><input value={input} onChange={e=>setInput(e.target.value)} disabled={!ready} placeholder="پیامت رو برای سکتور بنویس..."/><button className="sendBtn" disabled={busy||!input.trim()}>←</button></form></main>}
