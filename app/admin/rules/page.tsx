"use client";import{FormEvent,useEffect,useState}from"react";import{Icon,Mascot}from"../../ui";import{adminHeaders,bootstrapAdminSession}from"../adminClientSession";
export default function RulesPage(){const[token,setToken]=useState(""),[rules,setRules]=useState(""),[state,setState]=useState<"loading"|"ready"|"denied">("loading"),[msg,setMsg]=useState("");useEffect(()=>{void bootstrapAdminSession().then(async t=>{if(!t)return setState("denied");setToken(t);const r=await fetch('/api/admin/rules',{headers:adminHeaders(t),cache:'no-store'});const x=await r.json();if(!r.ok||!x.ok)return setState('denied');setRules(x.rules);setState('ready')})},[]);async function save(e:FormEvent){e.preventDefault();const r=await fetch('/api/admin/rules',{method:'PUT',headers:{'content-type':'application/json',...adminHeaders(token)},body:JSON.stringify({rules})}),x=await r.json();setMsg(x.ok?'قوانین گروه ذخیره شد ✨':'ذخیره انجام نشد');if(x.ok)setRules(x.rules)}if(state==='loading')return <main className="appShell"><section className="premiumPanel" style={{padding:20}}><Mascot small mood="thinking"/><h1>در حال بررسی مدیر...</h1></section></main>;if(state==='denied')return <main className="appShell"><section className="premiumPanel" style={{padding:20}}><Icon name="shield"/><h1>فقط مدیران گروه اجازه‌ی ورود دارند</h1><a className="primaryCta" href="/">بازگشت</a></section></main>;return <main className="appShell"><header className="appHeader"><a href="/admin" className="roundButton">←</a><div className="wordmark"><b>قوانین گروه</b><span>ویرایش سریع متن قوانین</span></div><span className="profileAvatar"><Icon name="shield"/></span></header><form className="premiumPanel" style={{padding:18,display:'grid',gap:12}} onSubmit={e=>void save(e)}><textarea rows={12} value={rules} onChange={e=>setRules(e.target.value)} placeholder="قوانین گروه..."/><small style={{color:'#aaa0c7'}}>تا ۳۰۰۰ نویسه؛ بعد از ذخیره، نسخه‌ی مدیریتی Family Bot از همین متن استفاده می‌کند.</small><button className="adminSave">ذخیره قوانین</button>{msg&&<div className="adminNotice">{msg}</div>}</form></main>}
