import {NextRequest,NextResponse} from "next/server";import {verifyFamilySession} from "@/lib/familySession";
export const runtime="nodejs";
function session(req:NextRequest){const a=req.headers.get("authorization")||"";return a.startsWith("Bearer ")?verifyFamilySession(a.slice(7)):null}
export async function POST(req:NextRequest){if(!session(req))return NextResponse.json({ok:false,error:"unauthorized"},{status:401});const key=process.env.GROQ_API_KEY||process.env.AI_API_KEY;if(!key)return NextResponse.json({ok:false,error:"stt_not_configured"},{status:503});try{const form=await req.formData(),file=form.get("audio");if(!(file instanceof File)||file.size<100)return NextResponse.json({ok:false,error:"audio_required"},{status:400});if(file.size>10*1024*1024)return NextResponse.json({ok:false,error:"audio_too_large"},{status:413});const body=new FormData();body.set("file",file,file.name||"voice.webm");body.set("model","whisper-large-v3-turbo");body.set("language","fa");body.set("response_format","json");const r=await fetch("https://api.groq.com/openai/v1/audio/transcriptions",{method:"POST",headers:{authorization:`Bearer ${key}`},body,signal:AbortSignal.timeout(20000)});const x=await r.json();if(!r.ok)throw new Error(x?.error?.message||"stt_failed");return NextResponse.json({ok:true,text:String(x.text||"").trim()},{headers:{"cache-control":"no-store"}})}catch(e){console.error("stt failed",e);return NextResponse.json({ok:false,error:"stt_failed"},{status:502})}}
