import {NextRequest,NextResponse} from "next/server";
import {createClient} from "@supabase/supabase-js";
import {sendDailyBriefingToChats} from "@/lib/dailyBriefing";
function db(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!u||!k)throw new Error('Family Core database is not configured');return createClient(u,k,{auth:{persistSession:false,autoRefreshToken:false}})}
function allowed(req:NextRequest){const expected=process.env.CRON_SECRET||'';const bearer=(req.headers.get('authorization')||'').replace(/^Bearer\s+/i,'');const header=req.headers.get('x-cron-secret')||'';return Boolean(expected&&(bearer===expected||header===expected))}
export async function POST(req:NextRequest){if(!allowed(req))return NextResponse.json({ok:false,error:'unauthorized'},{status:401});try{const body=await req.json().catch(()=>({}));const slot=body?.slot==='evening'?'evening':'morning';const rows=await db().from('families').select('bale_chat_id');if(rows.error)throw rows.error;const chats=(rows.data||[]).map(x=>x.bale_chat_id).filter(Boolean);const results=await sendDailyBriefingToChats(chats,slot);return NextResponse.json({ok:true,slot,total:results.length,sent:results.filter(x=>x.ok).length,failed:results.filter(x=>!x.ok).length})}catch(error){console.error('daily briefing cron failed',error);return NextResponse.json({ok:false,error:'daily_briefing_failed'},{status:500})}}
export async function GET(){return NextResponse.json({ok:true,service:'familybot-daily-briefing',schedule:'09:00 and 21:00 Asia/Tehran; activate via Supabase pg_cron after final deploy'})}
